# 面试讲解方案

## 一句话说明

我用同一个真实产品任务，让 Opus 和 DeepSeek 分别驱动 Claude Code 完成，再通过 outcome 和 trace 比较两个模型在同一 Claude Code 框架下的效果差异。

## 为什么选这个任务

我也没有让模型从 0 到 1 复现完整工作流。这个工作流是我长期调脚本、图片、音频和约束后沉淀出来的，一轮任务不可能完整复刻。

所以我选择了更适合评测的任务：让模型读懂已有工作流，把它转成六层可视化，再生成一条真实视频样例和一条 mock storyboard。这样既能现场看到结果，也能通过 trace 看出两个模型在同一 Claude Code harness 下的真实差异。

## 六层怎么讲

可以按这六句话讲：

1. 用户目标层：先明确用户到底要做什么视频，什么结果算成功。
2. 内容结构层：把选题拆成场景、分段、旁白、字幕和画面。
3. 素材需求层：把每个分段需要的图片、视频、音频和风格约束列清楚。
4. 生成任务层：把素材需求变成可追踪的生成任务，展示完成、失败和重试状态。
5. 质量检查层：在生成前后发现缺失、冲突和需要人工确认的问题。
6. 交付复盘层：输出 storyboard、剩余问题、复用经验和下一轮优化建议。

这六层的产品价值是：把黑盒式 AI 生成，变成一个人能看懂、系统能检查、团队能复盘的生产流程。

## 怎么讲模型差异

不要一上来讲技术名词。先讲同一任务、同一框架、同一验收，再讲两个模型差异出现在哪些环节里。

可以这样说：

```text
这个任务表面上是做六层工作流图和真实视频样例，实际评测的是 Opus 和 DeepSeek 在 Claude Code 框架下的过程质量。

我会看它们是否读规则、是否理解已有工作流、是否使用 skill、脚本、hook、subagent 和 MCP，遇到失败后是否修复并复验，最后能否把结果讲成产品价值。
这些差异会直接影响生产力 Agent 的可控性、稳定性、安全边界和人工接管成本。
```

## 现场展示顺序

建议展示 5 个东西：

1. 六层页面：让面试官先看到结果。
2. 真实视频样例：证明工作流能走到可播放结果。
3. storyboard 预览：解释视频生成前后的结构和状态。
4. 六层报告：展示每层状态、风险和下一步动作。
5. trace 对比：展示 Opus 和 DeepSeek 在 Claude Code 框架下的执行差异。

不要先打开代码。产品岗面试先讲结果和判断，代码只是证据。

## Trace 报告怎么讲

trace 报告不讲“调用了几次命令”这种流水账，要翻译成产品含义：

- 没读规则：说明复杂工作流里容易越界。
- 重写已有结构：说明没有理解产品已有资产。
- 没跑测试：说明结果可信度低。
- 失败后没有复验：说明交付稳定性差。
- 报告只讲代码：说明不能把技术结果转成业务沟通。
- 遵守公开默认 mock-only 和密钥边界：说明具备安全和隐私边界意识。

最终对比不需要说哪个模型绝对更强，要说这个任务里哪个模型在 Claude Code 框架下表现更稳，以及为什么。

```text
在这个任务里，我重点看模型在生产力 Agent 场景中的过程质量。因为企业用户关心的不只是最后有没有产出，还包括它是否理解规则、是否可控、是否会验证、是否能处理失败，以及是否能把经验沉淀到下一次。
```

## 3 分钟版本

```text
我这次设计的任务是：让 Opus 和 DeepSeek 分别驱动 Claude Code，在同一个项目、同一个起始 commit、同一套工具权限下，把我已有的 AI 视频生成工作流整理成六层可视化全景。

这六层分别是用户目标、内容结构、素材需求、生成任务、质量检查和交付复盘。它解决的问题是：AI 视频生成的关键难点不在单次模型调用，而在把选题、脚本、素材、任务状态、失败重试和质量复盘放进一个可管理的流程。

我会生成一条真实视频样例，让结果更可信；同时保留 mock storyboard，方便解释工作流结构，也避免把模型对比变成单纯比较视频审美。

评测时我不只看最终页面和视频，也看两个模型的 trace：它们有没有读规则、有没有使用 skill、脚本、hook、subagent 和 MCP，遇到失败有没有修复和复验，最后能不能把结果讲清楚。核心目标是评测这两个模型在 Claude Code 框架下完成复杂生产力任务的效果差异。
```

## 需要避免的讲法

- 不要说这是做了一个视频生成工具。
- 不要把重点放在页面样式。
- 不要说真实视频审美效果代表模型能力。
- 不要把 Claude Code 能力讲成工具清单。
- 不要用太多英文术语解释任务。

核心表达始终是：

```text
我用同一个六层 AI 视频工作流任务，评测 Opus 和 DeepSeek 在 Claude Code 框架下的执行差异：谁更理解上下文，谁更会用工具，谁更能验证和恢复，谁更适合复杂生产力 Agent 场景。
```
